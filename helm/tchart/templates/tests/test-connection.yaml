apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tchart.fullname" . }}-test-connection"
  labels:
    {{- include "tchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
