apiVersion: v1
kind: Service
metadata:
  name: {{ include "tchart.fullname" . }}
  labels:
    {{- include "tchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tchart.selectorLabels" . | nindent 4 }}
